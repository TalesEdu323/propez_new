/**
 * Defaults do builder para o servidor (hidratação pós-IA).
 * Reexporta o mapa canônico do cliente — sem dependências React.
 */
export { DEFAULT_PROPS } from '../../components/builder/defaultProps.js';
